import 'package:flutter/material.dart';

class CustomIcons {
  static final uvIcon = Icon(
    Icons.sunny,
    color: Colors.white,
    size: 60,
  );
  final Icon tempIcon = Icon(Icons.thermostat_auto_rounded);
  final Icon humIcon = Icon(Icons.water_drop);
}
